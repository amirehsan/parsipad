# Dependencies
node_modules/
.history

# OS files
.DS_Store
Thumbs.db

# IDE/Editor
.vscode/
.idea/
*.swp
*.swo

# Local configuration
.claude/
.vercel/

# Agent working state (task briefs, per-task reports, review diffs).
# The durable artefacts live in docs/superpowers/: the spec, the plan, and
# the execution log. .superpowers/sdd/ already carries its own "*" ignore,
# so this only makes the intent apply to the whole directory.
.superpowers/

# Build outputs
dist/
build/

# Environment files
.env
.env.local
*.local.json

# Logs
*.log
npm-debug.log*

# Landing page (deployed separately)
landing/

# Store assets (not needed in source)
store-assets/

# Internal planning docs
masterplan*.md
